{
  "version": 2,
  "buildCommand": "cd apps/frontend && npm run build:prod",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "npm install --legacy-peer-deps && cd apps/frontend && npm install --legacy-peer-deps && cd ../backend/core && npm install --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
